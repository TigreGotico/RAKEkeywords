name: Repo Health

on:
  pull_request:
    branches: [dev, master, main]
  workflow_dispatch:

jobs:
  repo_health:
    uses: OpenVoiceOS/gh-automations/.github/workflows/repo-health.yml@f81c5158260a0f9b21c9d8fbf2a83fa466df89f6
    with:
      version_file: 'RAKEkeywords/version.py'
